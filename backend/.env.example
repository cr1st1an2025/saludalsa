# CONFIGURACIÓN DE BASE DE DATOS
# IMPORTANTE: En producción (Railway), usa la DATABASE_URL que Railway provee automáticamente
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gestion_despachos

# SECRETO JWT - OBLIGATORIO
# ⚠️ CRÍTICO: Genera un secreto único y seguro (mínimo 32 caracteres aleatorios)
# Ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NO USAR EN PRODUCCIÓN: secreto_de_desarrollo_jwt_12345
JWT_SECRET=secreto_de_desarrollo_jwt_12345

# PUERTO DEL SERVIDOR
PORT=3002

# MODO DE DESARROLLO (SOLO PARA LOCAL)
# ⚠️ EN PRODUCCIÓN SIEMPRE DEBE SER false
DISABLE_AUTH=false
